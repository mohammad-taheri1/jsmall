// These codes are generated with jsmall CLI(https://github.com/MamadTaheri/jsmall)

// Axios is a better choice than fetch()

// first, define axios general configuration

// after that export them using httpClient object
